import React, { Commponent } from "react";
import UserList from "./presenter";

class Container extends Commponent {
  state = {};
  render() {
    return <UserList />;
  }
}

export default Container;
